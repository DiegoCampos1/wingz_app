"""Domain models for rides.

The ``Ride`` and ``RideEvent`` models are introduced in the next commit. This
module is intentionally left as a placeholder so the ``rides`` app is wired
into the project from the foundation onward.
"""
